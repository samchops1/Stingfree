fastapi==0.115.6
uvicorn==0.34.0
pydantic==2.10.4
presidio-analyzer==2.2.355
presidio-anonymizer==2.2.355
spacy==3.7.5
# Document text extraction for the file/folder PII discovery scanner.
pypdf==5.1.0
python-docx==1.1.2
openpyxl==3.1.5
# Windows-only: clipboard access for the machine-wide watcher. Marked with an
# environment marker so `pip install -r requirements.txt` still works on
# macOS/Linux for development (the clipboard watcher no-ops there).
pywin32==308; sys_platform == "win32"
# The large English model is installed separately (it is not a PyPI package
# you pin here):
#   python -m spacy download en_core_web_lg
